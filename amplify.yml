version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd Front
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: Front/dist
    files:
      - '**/*' 